/*
 * Declare an integer variable and assign it with the 
 * value 254 in hexadecimal format. Use Windows 
 * Calculator to find its hexadecimal representation.
*/

using System;

class HexadecimalInteger
{
    static void Main()
    {
        int hexValue = 0xFE; //Initialize the variable with 254 in hex
        Console.WriteLine(hexValue); //Printing the value of the variable
    }
}

